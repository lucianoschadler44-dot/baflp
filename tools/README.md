# tools

> Part of **BAFLP** — The Buenos Aires Framework for Artificial Legal Personhood.
> This README is mandatory metadata. Do not delete.

## Purpose
Helper tooling and configuration for contributors (linters, link-checker config, editor settings).

## Contents
`lychee.toml`, `markdownlint.json`, `.editorconfig` pointer, `vale/` prose-style config.

## Dependencies
Used by `scripts/validate.sh` and CI.

## Future evolution
Expands with the contributor base.
