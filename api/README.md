# api

> Part of **BAFLP** — The Buenos Aires Framework for Artificial Legal Personhood.
> This README is mandatory metadata. Do not delete.

## Purpose
Future structured exports (JSON) of concepts, registry records and the glossary, so other systems can consume the Framework.

## Contents
`schemas/`, generated `*.json` (git-ignored build output), `API.md` contract.

## Dependencies
Generated from `core/` and `registry/` by build scripts.

## Future evolution
May graduate to a versioned public data API.
