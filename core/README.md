# core

> Part of **BAFLP** — The Buenos Aires Framework for Artificial Legal Personhood.
> This README is mandatory metadata. Do not delete.

## Purpose
Canonical, citable source of truth for the Framework. Everything else references these objects; nothing here is duplicated elsewhere.

## Contents
Sub-modules: `ontology/`, `taxonomy/`, `concepts/`, `principles/`, `rfcs/`.

## Dependencies
Consumed by `model-law/`, `comparative-law/`, `research/`, `registry/`, `website` build.

## Future evolution
Grows by adding versioned knowledge objects via RFC, never by editing approved definitions in place.
