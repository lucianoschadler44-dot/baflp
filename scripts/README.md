# scripts

> Part of **BAFLP** — The Buenos Aires Framework for Artificial Legal Personhood.
> This README is mandatory metadata. Do not delete.

## Purpose
Automation that builds, validates and scaffolds the project. Every script is documented; no undocumented scripts allowed.

## Contents
`build.sh`, `validate.sh`, `new-concept.sh`, `new-paper.sh`, `bootstrap.sh`.

## Dependencies
Depend on Quarto, lychee (link check), pandoc (via Quarto).

## Future evolution
Consolidate into a single `make`-style entrypoint over time.
