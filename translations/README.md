# translations

> Part of **BAFLP** — The Buenos Aires Framework for Artificial Legal Personhood.
> This README is mandatory metadata. Do not delete.

## Purpose
Synchronised non-English editions. English is the master; translations mirror structure and must not drift semantically.

## Contents
`es-AR/` (Argentina), `pt-BR/` (Brazil), each mirroring `core/` paths + a `STATUS.md` sync table.

## Dependencies
Source of truth is always the English object; translations reference the same concept IDs.

## Future evolution
Adds translation-coverage checks in CI.
